# Ingestion CLI (your Mac). Run from pharma-rag-web/:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r ingest/requirements.txt
# Torch installs with MPS (Apple GPU) support automatically on macOS.
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
aiosqlite>=0.20
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
pymilvus>=2.5.0
sentence-transformers>=3.0
torch>=2.3
PyMuPDF>=1.24
python-docx>=1.1
google-api-python-client>=2.130
google-auth>=2.30
